class AppValidation {
  /*
  static bool validateSignUp({
    required AuthRequestModel authRequestModel,
  }) {
    if (authRequestModel.name.isEmpty) {
      DataHelper.showToast(
        AppStrings.enterName.tr,
      );
      return false;
    }

    if (authRequestModel.dob.isEmpty) {
      DataHelper.showToast(
        AppStrings.enterDob.tr,
      );
      return false;
    }

    if (authRequestModel.email.isEmpty) {
      DataHelper.showToast(
        AppStrings.enterEmail.tr,
      );
      return false;
    }

    if (!DataHelper.emailValidation(
      authRequestModel.email,
    )) {
      DataHelper.showToast(
        AppStrings.enterValidEmail.tr,
      );
      return false;
    }

    if (authRequestModel.password.isEmpty) {
      DataHelper.showToast(
        AppStrings.enterPass.tr,
      );
      return false;
    }

    return true;
  }
  */
}